node_modules
dist
coverage
package-lock.json

# Generated by `npm run data:import`, which writes it with
# JSON.stringify(data, null, 2). If Prettier reformatted it, every import would
# produce a diff against the committed file and the weekly refresh workflow
# would open a PR every week for a change that isn't one. Never format it.
src/data/game-data.json

# Upstream snapshots, git-ignored but present locally after an import.
scripts/import/sources/.cache
